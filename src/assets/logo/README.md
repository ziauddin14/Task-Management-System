# Dawat-e-Islami branding assets (pending)

The client has not yet provided official logo/branding assets (`docs/01-architecture.md`'s
client-responsibility item; also noted in `docs/09-frontend-features.md`). The login screen and
header currently use a placeholder brand color (`tailwind.config.js`'s `brand` colors) with no
logo image. Once assets arrive, drop them here and reference them from the relevant components.
