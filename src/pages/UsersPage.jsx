import React from 'react'; // explicit import — see src/App.jsx's comment for why

// Placeholder — real Admin user management UI is built in a later sub-phase.
function UsersPage() {
  return <h1>Users</h1>;
}

export default UsersPage;
