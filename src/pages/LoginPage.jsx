import React from 'react'; // explicit import — see src/App.jsx's comment for why

// Placeholder — real Google Sign-In UI is built in Phase 10.2.
function LoginPage() {
  return <h1>Login</h1>;
}

export default LoginPage;
